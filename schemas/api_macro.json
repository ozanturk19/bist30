{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "api_macro",
  "description": "Schema for /api/macro response",
  "type": "object",
  "required": ["items", "cached"],
  "additionalProperties": true,
  "properties": {
    "items":  {"type": "array"},
    "cached": {"type": "boolean"},
    "stale":  {"type": "boolean"}
  }
}
